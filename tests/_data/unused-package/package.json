{
  "private": true,
  "name": "@cyclonedx/cyclonedx-eslint-testing-unused-package",
  "version": "0.0.1",
  "description": "dummy package that is imported but not used, to test tree-shaking behavior",
  "license": "dummy-license",
  "type": "module",
  "main": "./lib/index.js",
  "files": [
    "lib"
  ],
  "sideEffects": false,
  "scripts": {
    "prebuild": "node -r fs -e \"fs.rmSync('package.tgz',{force:true})\"",
    "build": "npm pack && mv *.tgz package.tgz"
  }
}

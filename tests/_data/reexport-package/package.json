{
  "private": true,
  "name": "@cyclonedx/cyclonedx-eslint-testing-reexport-package",
  "version": "0.0.1",
  "description": "this is a dummy package for showcasing purposes only.",
  "type": "module",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "files": [
    "src",
    "README.md"
  ],
  "dependencies": {
    "custom-package": "file:../custom-package/package.tgz"
  },
  "sideEffects": false,
  "scripts": {
    "prebuild": "node -r fs -e 'fs.rmSync(`package.tgz`,{force:true})'",
    "build": "npm pack && mv *.tgz package.tgz"
  }
}

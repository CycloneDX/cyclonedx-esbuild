# showcase overriding the RootComponent's name/version

this package sets `rootComponentAutodetect: false` and provides `rootComponentName`/`rootComponentVersion`
explicitly, instead of letting the plugin derive them from the nearest `package.json`.

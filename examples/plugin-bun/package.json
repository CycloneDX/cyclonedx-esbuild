{
  "private": true,
  "name": "@cyclonedx/cyclonedx-esbuild-example-plugin-bun",
  "version": "0.1.0-dev",
  "description": "demo as a plugin for bun",
  "engines": {
    "bun": "*"
  },
  "main": "./dist/app.js",
  "dependencies": {
    "@popperjs/core": "^2",
    "blueimp-gallery": "^3",
    "bootstrap": "^5"
  },
  "devDependencies": {
    "@cyclonedx/cyclonedx-esbuild": "file:../.."
  },
  "scripts": {
    "build": "bun build.ts"
  }
}
